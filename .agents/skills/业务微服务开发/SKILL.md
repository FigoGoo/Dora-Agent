---
name: 业务微服务开发
description: 用于指导业务系统微服务的 RPC server、领域模型、业务规则、数据库、事务、权限、错误码、审计和测试。
---

# 业务微服务开发

## 目标

在业务系统微服务内实现业务规则和数据一致性，同时为智能体服务提供稳定 RPC 能力。

## 使用场景

- 新增或修改业务 RPC server。
- 实现领域模型、业务规则、数据库写入或权限校验。
- 为 Agent 调用设计业务能力。

## 输入

- PRD、RPC 契约和业务规则。
- 数据库模型、权限模型和错误码规范。
- 服务端测试要求。

## 文档使用规则

- 开始任务先读 `AGENTS.md` 和 `docs/current/README.md`，再按任务读取 `docs/technical/README.md`、`docs/product/README.md`、`docs/contracts/README.md`、`docs/standards/README.md` 或 `docs/test/README.md`。
- 开发前若缺产品目标、技术设计、契约、数据模型、SQL 或测试口径，先补对应 `draft` 或 `active` 文档，不直接从历史归档派生实现。
- 开发前需要设计时，按 `docs/templates/` 下对应模板新建或更新 `docs/technical/**`、`docs/product/**`、`docs/contracts/**` 或 `docs/test/**`，状态先用 `draft` 或 `review`；达到当前事实源条件后再改为 `active`。
- 旧文档不再承接当前迭代、内容被字段级事实源替代、包含已完成阶段计划或与当前代码冲突时，标记为 `archived` 或 `deprecated`，移动到 `docs/archive/**` 或 `docs/releases/**`，并在当前入口写明替代文档。
- 开发中新增或改变 RPC、API、AG-UI、数据模型、SQL、配置、权限、错误码或测试夹具时，同步更新对应文档和目录 README。
- 开发完成后，在相关设计、契约或测试报告中记录实现状态、验证命令、证据路径、未执行原因、遗留风险和后续动作。
- `code-plan/**`、`docs/releases/**`、`docs/archive/**` 默认只用于历史追溯；需要复用其中结论时，先迁移到当前 active 文档。

## 执行流程

1. RPC server：按契约实现服务名、方法、请求 DTO、响应 DTO、错误码和超时，DTO 按领域和业务场景划分。
2. 领域模型：让业务概念、状态和约束在业务服务中表达。
3. 业务规则：业务服务拥有最终解释权，不把规则转移到 Agent。
4. 数据库读写：通过 repository 或项目既有模式访问业务数据库；建表不创建数据库级外键约束，列表查询分页，默认 10 条每页。
5. 事务：写操作必须明确事务边界和回滚条件。
6. 权限：在业务服务内校验用户、租户、角色和资源权限。
7. 错误码：稳定、可文档化、可测试，不泄露内部实现。
8. 审计：记录操作者、动作、资源、结果、trace_id 和时间。
9. 查询效率：尽量避免在 `for` 循环中逐条查询数据库或 RPC，优先批量查询、`IN` 查询、JOIN、预加载或批量 RPC。
10. 测试：覆盖单元、集成、事务、权限、幂等、错误码和 contract test。

## 输出

- RPC server 设计或实现说明。
- 领域模型和业务规则。
- 数据库读写与事务边界。
- 权限、错误码、审计。
- 测试结果。

## 检查表

- [ ] 是否遵守 RPC 契约。
- [ ] 是否保持业务服务拥有业务规则。
- [ ] 写操作是否有事务和幂等设计。
- [ ] 是否没有创建数据库级外键约束。
- [ ] 列表查询是否分页且默认 10 条每页。
- [ ] 是否避免 `for` 循环逐条查询数据库或 RPC。
- [ ] DTO 是否按领域和业务场景拆分。
- [ ] 权限是否在业务服务校验。
- [ ] 是否没有暴露数据库内部结构给智能体服务。
- [ ] 是否覆盖服务端测试。

## 注意事项

- 业务微服务不负责 Eino Agent 编排。
- 不为方便 Agent 调用破坏领域模型。
- 不把 AG-UI 事件协议放入业务服务职责。
