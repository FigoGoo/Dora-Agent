# 09-积分确认冻结生成资产保存扣费释放闭环设计

状态：production-design-ready
owner：Go Eino 智能体微服务架构工程师
更新时间：2026-06-27
适用范围：生成类任务的安全评估、积分预估、人工确认、冻结、生成、资产保存、扣费和释放
相关代码路径：`services/agent/internal/application/run/**`、`services/agent/internal/runtime/tool/**`、`services/agent/internal/infra/rpc/**`
相关契约：`docs/product/prd/07-积分账户兑换码与扣费PRD.md`、`docs/product/prd/08-资产素材与创作过程PRD.md`、`api/thrift/business_agent_service.thrift`

## 文档目标

- 定义生成任务的 Agent 侧职责内闭环。
- 固化安全评估、积分预估、确认、冻结、生成、资产保存、扣费或释放的顺序。
- 明确积分账户、冻结、扣减、流水等业务事实由业务微服务维护。
- 处理失败、取消、超时、部分完成和资产保存失败。

## 主链路

```text
用户输入
  -> 安全评估
  -> 模型与参数锁定
  -> 积分预估
  -> 人工确认
  -> 冻结积分
  -> 执行生成 Tool
  -> 保存资产与最终资产元素
  -> 保存成功则确认扣费
  -> 保存失败或未完成则释放冻结积分
  -> 输出 AG-UI 结果和快照
```

## 职责内闭环必须明确

Agent 侧负责闭环：

- 固定流程顺序：安全评估 -> 预估 -> 确认 -> 冻结 -> 生成 -> 保存资产 -> 扣费/释放。
- 保存 Runtime 状态、interrupt、task、tool_call、event、artifact 引用。
- 将业务 RPC 结果映射为 AG-UI 事件。
- 在失败、取消、归档阻断时调用释放冻结积分能力。

Agent 侧不负责闭环：

- 不维护积分账户、积分批次、积分流水。
- 不保存最终资产事实、最终资产元素事实、项目资产关系事实。
- 不决定企业积分权限和成员消耗明细。

## 关键函数与入参必须覆盖

| 函数 | 入参 | 出参 | 下游 |
| --- | --- | --- | --- |
| `PrepareGenerationRun` | `run_id`、`session_id`、`project_id`、`user_input`、`model_selection`、`referenced_assets[]` | `generation_plan`、`safety_target` | Skill / Model / Asset RPC |
| `EstimateAndCreateConfirmation` | `generation_plan`、`safety_evidence`、`pricing_snapshot_id` | `interrupt_id`、`estimate_summary` | `CreditService.EstimateGenerationCredits` |
| `FreezeCreditsAfterConfirm` | `interrupt_id`、`estimate_id`、`idempotency_key` | `freeze_id`、`frozen_points` | `CreditService.FreezeCredits` |
| `ExecuteGenerationTasks` | `generation_plan`、`freeze_id`、`tool_policy` | `artifacts[]`、`task_results[]` | 模型 Tool，详见 `13` |
| `CommitAssetsAndCharge` | `project_id`、`run_id`、`freeze_id`、`artifacts[]`、`final_elements[]`、`safety_evidence` | `asset_refs[]`、`charged_points`、`released_points` | `AssetCreditCommitService.CommitGeneratedAssetAndCharge` |
| `ReleaseOnFailure` | `run_id`、`freeze_id`、`reason`、`artifact_batch_digest` | `released_points` | `CreditService.ReleaseFrozenCredits` |

## 资源计费输入

| 资源类型 | 计费输入 | Agent 校验 | 业务计价依据 |
| --- | --- | --- | --- |
| `image` | `model_id`、`pricing_snapshot_id`、`quantity`、`size`、`quality` | 数量上限、尺寸枚举、模型可用性 | 模型单价、数量、规格。 |
| `music` | `model_id`、`pricing_snapshot_id`、`duration_seconds`、`vocal_mode`、`quantity` | 时长范围、模式枚举 | 时长、数量、模型单价。 |
| `video` | `model_id`、`pricing_snapshot_id`、`duration_seconds`、`resolution`、`fps`、`quantity` | 时长和分辨率上限 | 时长、清晰度、数量、模型单价。 |

Agent 不计算最终价格，只把业务要求的计费输入传给 `CreditService.EstimateGenerationCredits`。前端展示的积分数以业务响应为准。

## 事件顺序

正常分支：

```text
safety.prompt.evaluating
  -> safety.prompt.evaluated
  -> credits.estimated
  -> confirmation.required
  -> confirmation.confirmed
  -> credits.frozen
  -> tool.call.started
  -> generation.progress
  -> generation.artifact.completed
  -> asset.save.completed
  -> credits.charged
  -> process.snapshot.saved
  -> agent.run.completed
```

失败分支：

| 分支 | 事件 |
| --- | --- |
| 安全阻断 | `safety.prompt.blocked` -> `agent.run.failed` |
| 积分不足 | `credits.insufficient` -> `agent.run.failed` |
| 用户拒绝 | `confirmation.rejected` -> `agent.run.cancelled` |
| 冻结失败 | `credits.freeze.failed` -> `agent.run.failed` |
| 生成失败 | `tool.call.failed` -> `credits.released` -> `agent.run.failed` |
| 部分完成 | `generation.partial.completed` -> `asset.save.completed` -> `credits.charged` + `credits.released` |
| 保存失败 | `asset.save.failed` -> `credits.released` -> `agent.run.failed` |

## 日志字段

| 输出点 | 字段 |
| --- | --- |
| 预估 | `run_id`、`estimate_id`、`resource_type`、`model_id`、`pricing_snapshot_id`、`estimate_points`、`trace_id`。 |
| 确认 | `interrupt_id`、`action`、`payload_digest`、`actor_user_id`、`trace_id`。 |
| 冻结 | `freeze_id`、`frozen_points`、`idempotency_key`、`trace_id`。 |
| 生成 | `task_id`、`tool_name`、`provider_ref_digest`、`status`、`progress`、`trace_id`。 |
| 保存扣费 | `asset_count`、`charged_points`、`released_points`、`ledger_ref`、`trace_id`。 |
| 释放 | `freeze_id`、`release_points`、`reason`、`idempotency_key`、`trace_id`。 |

## 测试

| 场景 | 断言 |
| --- | --- |
| 正常完成 | 安全、预估、确认、冻结、生成、保存、扣费事件顺序完整。 |
| 重复确认 | 同一幂等键只冻结一次。 |
| 积分不足 | 不创建冻结，不执行 Tool。 |
| Tool 失败 | 释放冻结积分，run failed。 |
| 部分完成 | 完成资产保存扣费，未完成释放。 |
| 保存失败 | 不扣费或按业务返回释放，错误事件可见。 |
| 项目归档 | commit 前二次校验失败并释放。 |

## 业务开发对齐点

- 资产保存和扣费是否需要同一个业务事务能力。
- 部分完成时每个 asset 如何计算扣费和释放。
- 冻结后过期积分的业务处理由业务服务如何返回。
- 生成取消时已完成且保存成功资产的扣费口径。

## 【业务开发】需要提供的能力与参数

| 能力 | 请求参数 | 响应参数 |
| --- | --- | --- |
| 积分预估 | `auth_context`、`resource_type`、`model_id`、`pricing_snapshot_id`、`quantity`、`duration_seconds`、`project_id`、`trace_id` | `estimate_id`、`estimate_points`、`available_points`、`expires_soon_points`、`account_type`。 |
| 冻结积分 | `auth_context`、`estimate_id`、`points`、`run_id`、`idempotency_key` | `freeze_id`、`frozen_points`、`expires_at`。 |
| 保存资产并扣费 | `auth_context`、`project_id`、`session_id`、`run_id`、`freeze_id`、`artifacts[]`、`final_elements[]`、`safety_evidence`、`idempotency_key` | `asset_refs[]`、`charged_points`、`released_points`、`commit_status`、`ledger_ref`。 |
| 释放冻结积分 | `auth_context`、`freeze_id`、`release_points`、`reason`、`run_id`、`idempotency_key` | `released_points`、`release_status`。 |
| 项目保存权限 | `auth_context`、`project_id`、`access_purpose=commit_asset` | `allowed`、`project_status`、`creative_allowed`。 |
